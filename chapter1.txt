hola soy archivo que sera subido a git

cd Desktop ==> moverse al escritorio mediante la terminal
mkdir folderName ==> crear carpeta
ls ==> mostrar el contenido de la carpeta
ls -a ==> muestra incluso archivos ocultos
touch fileName.xx ==> crear un archivo con .xx fotmato
code fileName.xx ==> abrir con vs code el archivo fileName.xx

git init ==> iniciar git
git status ==> 
git add fileName ==> agregar un archivo al "Staging Area" (documentos listos para hacer commit) 
git add . ==> agregar al "Stagin area" todos los archivos en el directorio actual
git commit ==> para hacer el commit a github
git commit -m "bla bla" ==> agregar un mensaje en el commit
git log ==> para ver que commit se ha hecho
git checkout ==> para descartar los cambios en el directorio de trabajo. Dar un paso atras (equivalente a un ctrl + z)
git diff fileName.xx ==> para comparar los ultimos cambios realizados

-> Luego de crear el repositorio en Git hub, se deben subir los avances de nuestro repositorio local al repositorio remoto
git push -u <remoteName> <branchName>

git ignore

-> para descargar y crear una copia local de un repositorio remoto se usa el comando clone, debemos ubicarnos en el floder donde
deseamos ubicar la copia del repositorio y desde ahi correr el comando
git clone url

-> Branching and Merging: to develope features and colaborates on a codebase
git branch <nameOfBranch> ==> para crear esta nueva rama
git branch ==> para ver que ramas existen, el * nos indica en que rama estamos
git checkout <nameOfBranch> ==> para cambiar de rama
git merge <nameOfBranch> ==> para agregar/sobreescribir los cambios realizados en una rama a otra. 
                            debo estar ubicado en la rama sobre la que quiero los cambios. 
                            comando para salir => :q!

cambio realizado en la rama basura

-> Forking and Pull requests
Forking: Es diferente a Clone, ya que clonar es crear una copia local, mientras Forking es crear una copia en el repositorio remoto.
luego de haber hecho un Fork, se puede realiar un Clone de esta copia. Luego de haber solucionado bugs o haber hecho adiciones o mejoras,
se puede realizar un Pull request, para agregar los cambios realizados en el codigo original. Este Pull request, debe ser evaluado y aceptado
por el dueño del codigo original, por esto un Pull request es diferente a un Push